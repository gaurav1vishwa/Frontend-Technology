

console.log(a);

let a =29;


hoisting(23);

function hoisting(a) {
    console.log(a);
    
}

          // MEMORY MANAGEMENT  AND HOW THE MEMORY WILL BE ALLOCATGED AND USED


          // arraow function is never hoisted but function declaration is hoisted and variable declaration is also hoisted but not the initialization

          // in case of var the variable declaration is hoisted but not the initialization and in case of let and const the variable declaration is also hoisted but it is in temporal dead zone until it is initialized

          

